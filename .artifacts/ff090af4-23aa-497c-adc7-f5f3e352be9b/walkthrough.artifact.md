# AGP Upgrade to 8.13.2 Walkthrough

I have upgraded the Android Gradle Plugin (AGP) to version **8.13.2** to resolve the Kotlin metadata parsing warning and ensure full compatibility with Kotlin 2.3.0.

## Changes Made

### 1. AGP Upgrade
- Updated `libs.versions.toml` to set `agp = "8.13.2"`.
- This version includes **R8 8.13.19**, which is required to correctly parse metadata generated by **Kotlin 2.3.0**.

### 2. Verification
- **Gradle Sync**: Successful.
- **Project Build**: Successful (`gradlew assembleDebug`).
- **Warning Removal**: The "An error occurred when parsing kotlin metadata" warning is no longer present in the build logs.

### 3. Version Control
- **Git Commit**: Created a local commit with the message: `Upgrade Android Gradle Plugin to 8.13.2 and implement Unity Ads Mediation`.
- **Note on Git Push**: As mentioned before, you need to manually perform `git push` to upload these changes to your GitHub repository.

## Final Status
The project is now fully compatible with Kotlin 2.3.0, and the Unity Ads mediation is correctly configured with the latest recommended APIs.
